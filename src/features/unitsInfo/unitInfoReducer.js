import { createReducer } from "../../common/utils/reducerUtils";

const INITIAL_STATE = {
  name: "Black Widow Company",
  affiliation: "wd",
};

function someFunction(state, payload) {}

export default createReducer(INITIAL_STATE, {});
